# Sanity CMS Configuration
# Get these values from your Sanity project dashboard at https://www.sanity.io/manage
REACT_APP_PROJECT_ID=your_sanity_project_id
REACT_APP_DATASET=production

# API Configuration (if using Sanity mutations API)
REACT_APP_BASE_URL=https://your-project-id.api.sanity.io/v2024-01-01/data/mutate/production
REACT_APP_BEARER_TOKEN=your_sanity_bearer_token

# Instructions:
# 1. Copy this file to .env (without .example)
# 2. Replace the placeholder values with your actual Sanity credentials
# 3. To get your project ID and dataset:
#    - Go to https://www.sanity.io/manage
#    - Select your project
#    - Find the Project ID in the project settings
# 4. To get a bearer token:
#    - Go to your Sanity project settings
#    - Navigate to API section
#    - Create a new token with appropriate permissions
